language: python

python:
  - "3.6"
  - "3.7"
  - "3.8"

services:
  - neo4j

install:
  - pip install .[dev]

script:
  - pytest --cov=./

after_success:
  - codecov

branches:
  only: 
    - master
